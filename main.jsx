import React from "react";
import ReactDOM from "react-dom/client";
import OpspecsPortal from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OpspecsPortal />
  </React.StrictMode>
);